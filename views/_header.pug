header.header
    nav.nav.nav--tours
        a.nav__el(href="/") MiniCRM - LIST OF COMPANIES
    .header__logo
        img(src="" alt="")
    nav.nav.nav--user
        if user
            a.nav__el.nav__el--logout.nav__el--cta(href="/logout") Log out
        else
            a.nav__el.nav__el--cta(href='/login') Log In
            //- button.nav__el.nav__el--cta Sign Up
