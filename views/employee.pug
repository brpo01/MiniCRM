extends base

block content
    main.main
        .card-container
            each employee in companies.employees
                .card
                    .card__header
                        .card__picture
                            .card__picture-overlay &nbsp;
                            //- img.card__picture-img(src=``, alt=``)
                        h3.heading-tertirary
                            span= `${employee.firstName} ${employee.lastName}`

                    .card__details
                        h4.card__sub-heading= `Email -  ${employee.email}`
                        
